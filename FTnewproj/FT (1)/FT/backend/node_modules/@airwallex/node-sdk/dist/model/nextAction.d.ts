import { CandidateAccount } from './candidateAccount';
export interface NextAction {
    candidate_accounts?: Array<CandidateAccount>;
    micro_deposit_count: number;
    remaining_attempts: number;
    type: NextAction.TypeEnum;
    verification_type: NextAction.VerificationTypeEnum;
}
export declare namespace NextAction {
    const validTypeEnum: readonly ["verify_micro_deposits", "confirm_account", "auth", "update_mandate", "external_authorize"];
    type TypeEnum = (typeof validTypeEnum)[number] | 'UNKNOWN';
    const validVerificationTypeEnum: readonly ["PLAID", "TRUELAYER", "EGIRO"];
    type VerificationTypeEnum = (typeof validVerificationTypeEnum)[number] | 'UNKNOWN';
    const discriminator: string;
    const attributeTypeMap: Array<{
        name: string;
        baseName: string;
        type: string;
    }>;
}
//# sourceMappingURL=nextAction.d.ts.map