import { Duration } from './duration';
import { ZoneOffset } from './zoneOffset';
export interface ZoneOffsetTransition {
    date_time_after?: Date;
    date_time_before?: Date;
    duration?: Duration;
    gap?: boolean;
    instant?: Date;
    offset_after?: ZoneOffset;
    offset_before?: ZoneOffset;
    overlap?: boolean;
}
export declare namespace ZoneOffsetTransition {
    const discriminator: string;
    const attributeTypeMap: Array<{
        name: string;
        baseName: string;
        type: string;
    }>;
}
//# sourceMappingURL=zoneOffsetTransition.d.ts.map